# Add the next two lines to firejailed-tor-browser.local to enable better desktop integration
include /etc/firejail/whitelist-common.inc

private-bin cat,cp,cut,getconf,id,kdialog,ln,mkdir,pwd,readlink,realpath,sed,tail,test,update-desktop-database,xmessage,xmessage,zenity

# I'm not sure if this next line is effective for this application so uncomment to test if desired.
#blacklist /run/user/*/bus
